name: motrainer
channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - scipy
  - tensorflow
  - scikit-learn
  - pandas <= 2.0.0
  - scikit-optimize
  - ipykernel
  - matplotlib
  - pip
  - pip:
    - -e ./
